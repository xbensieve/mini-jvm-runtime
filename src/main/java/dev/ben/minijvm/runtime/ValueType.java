package dev.ben.minijvm.runtime;

/**
 * Enumeration of supported runtime value categories in the Mini JVM.
 */
public enum ValueType {
    INT,
    LONG,
    FLOAT,
    DOUBLE,
    REFERENCE,
    NULL
}
